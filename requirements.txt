# ─── Core pipeline ────────────────────────────────────────────────────────────
# Required by: rainfall_trend_analysis_v3/v4.py, all generate_*.py, rta/ package
# Tested on Python 3.11.15 (2026-05); minimum versions listed
numpy>=1.21          # tested 2.4.6
pandas>=1.3          # tested 3.0.3
scipy>=1.7           # tested 1.17.1
matplotlib>=3.4      # tested 3.10.9
openpyxl>=3.0        # tested 3.1.5

# ─── Extended pipeline ────────────────────────────────────────────────────────
# Required by: Comparative_4MMK.py only (standalone; not part of primary pipeline)
statsmodels>=0.13

# ─── Optional spatial (rta_v5/ only) ─────────────────────────────────────────
# Required by: rta_v5/spatial_interpolation_v5.py (imported conditionally;
# gracefully skipped if absent — CRS reprojection and shapefile reading disabled)
# pyproj>=3.2        # tested 3.7.2
# pyshp>=2.2         # tested 3.0.8
